import json
import sys
from pprint import pprint

with open(sys.argv[1]) as data_file:
  data = json.load(data_file)
for i in range(0,len(data['nodes'])):
  print(data['nodes'][i]['capabilities'].encode("ascii"), data['nodes'][i]['mac'][0].encode("ascii"))
